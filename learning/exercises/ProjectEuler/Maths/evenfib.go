package maths

//Evenfib : Returns a sum of the first x number of even numbers in the Fibonnaci sequence
//guess is the length of the Fib sequence
//x is the value that the Fib numbers should not exceed
func Evenfib(x int) int {

	//fterm := 1
	//	sterm := 2
	//	sum := 0

	/*	for fib < x {
			fib := fterm + sterm
			fmt.Println("FIB", fib)
			p := fib % 2
			fmt.Printf("FIB mod 2 %v \n", p)
			if fib%2 == 0 {
				sum += fib
				fmt.Println("Sum @ IF --", sum)
			}

			fterm = sterm
			sterm = fib

		}
	*/
	return 42

}
